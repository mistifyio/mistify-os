# Configure Loopback
auto lo
iface lo inet loopback

auto eno3
iface eno3 inet dhcp
  # The interface needs some time to wake up.
  pre-up ip link set $IFACE up
  pre-up echo waiting....
  pre-up sleep 4

